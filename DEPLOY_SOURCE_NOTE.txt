Direct source deployment enabled. The application source is committed directly to GitHub; Vercel should use the standard Next.js install/build flow.

Deployment trigger: compact week dropdown UI.
Deployment trigger: hide player proof panel.
Deployment trigger: player confirmation-only screen.
Deployment retry check.
